# AMD AI Server Stack - Environment Configuration
# Copy this file to .env and modify paths for your system

# =============================================================================
# HOST PATHS - MODIFY THESE FOR YOUR SYSTEM
# =============================================================================

# Base path for AI models
MODELS_BASE=/home/youruser/ai/models

# Ollama models (GGUF format)
OLLAMA_MODELS=${MODELS_BASE}/gguf

# Speech-to-text models
STT_MODELS=${MODELS_BASE}/stt

# Text-to-speech models
TTS_MODELS=${MODELS_BASE}/tts

# ComfyUI installation path (if using bind mount)
COMFYUI_PATH=/home/youruser/programs/ComfyUI

# =============================================================================
# GPU CONFIGURATION
# =============================================================================

# ROCm GFX version override
# Common values:
#   gfx1100 (RX 7900 XTX/XT) -> 11.0.0
#   gfx1101 (RX 7700 XT/7800 XT) -> 11.0.1
#   gfx1102 (RX 7600) -> 11.0.2
#   gfx90a (MI200 series) -> 9.0.10
#   gfx908 (MI100) -> 9.0.8
HSA_OVERRIDE_GFX_VERSION=11.0.1

# ROCm installation path
ROCM_PATH=/opt/rocm

# GPU device selection (0 = first GPU)
HIP_VISIBLE_DEVICES=0

# PyTorch ROCm architecture
PYTORCH_ROCM_ARCH=gfx1101

# =============================================================================
# SERVICE PORTS
# =============================================================================

# Ollama API
OLLAMA_PORT=11434

# Open WebUI
OPENWEBUI_PORT=3000

# Whisper STT
WHISPER_PORT=9000

# Whisper Enhanced (with Ollama post-processing)
WHISPER_ENHANCED_PORT=9001

# ComfyUI
COMFYUI_PORT=8188

# TTS
TTS_PORT=8880

# PyTorch Jupyter
JUPYTER_PORT=8888

# =============================================================================
# MODEL DEFAULTS
# =============================================================================

# Default Whisper model (tiny, base, small, medium, large, large-v3)
WHISPER_MODEL=base

# Default language for STT (leave empty for auto-detect)
WHISPER_LANGUAGE=

# =============================================================================
# NETWORK
# =============================================================================

# Docker network name
DOCKER_NETWORK=ai-stack
